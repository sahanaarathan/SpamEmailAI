[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

[isort]
profile = black
line_length = 100

[flake8]
max-line-length = 100
ignore = E203, W503
exclude = .git, __pycache__, .venv, venv, dist, build
